# HTTP client — used by AgentClient and Slack delivery
httpx>=0.28.1,<1.0

# Timezone-aware datetime arithmetic
pytz>=2026.1.post1

# Postgres client — used for job-run state and audit log
# psycopg2-binary ships the shared library; use psycopg2 for compile-from-source envs.
psycopg2-binary>=2.9.12,<3.0

# Environment variable loading from .env files (development convenience)
python-dotenv>=1.2.2,<2.0
